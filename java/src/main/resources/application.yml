server:
  port: 8080

spring:
  application:
    name: zsxq-sdk-demo

zsxq:
  token: ${ZSXQ_TOKEN:YOUR_ZSXQ_TOKEN}
  group-id: ${ZSXQ_GROUP_ID:YOUR_GROUP_ID}
  timeout: 10000
  retry-count: 3
  retry-delay: 1000
